# /etc/mailcap file for Pardus
# Copyright 2007-2010 TUBITAK/UEKAE
#
# Released under GPLv2

application/x-pisi; pm-install '%s'; -n "$DISPLAY"
application/gzip; ark '%s'; -n "$DISPLAY"
application/x-gzip; ark '%s'; -n "$DISPLAY"
application/gzipped-patch; ark '%s'; -n "$DISPLAY"
application/zip; ark '%s'; -n "$DISPLAY"
application/x-zip; ark '%s'; -n "$DISPLAY"
application/tar; ark '%s'; -n "$DISPLAY"
application/x-tar; ark '%s'; -n "$DISPLAY"
application/bzip2; ark '%s'; -n "$DISPLAY"
application/x-bzip2; ark '%s'; -n "$DISPLAY"

# Powerpoint
application/x-powerpoint; ooimpress '%s'; -n "$DISPLAY"
application/mspowerpoint; ooimpress '%s'; -n "$DISPLAY"
application/x-mspowerpoint; ooimpress '%s'; -n "$DISPLAY"
application/vnd.x-mspowerpoint; ooimpress '%s'; -n "$DISPLAY"
application/ms-powerpoint; ooimpress '%s'; -n "$DISPLAY"
application/vnd.ms-powerpoint; ooimpress '%s'; -n "$DISPLAY"
application/vnd.ms-pps; ooimpress '%s'; -n "$DISPLAY"

# MS Word
application/doc; oowriter '%s'; -n "$DISPLAY"
application/msword; oowriter '%s'; -n "$DISPLAY"
application/x-msword; oowriter '%s'; -n "$DISPLAY"
application/ms-word; oowriter '%s'; -n "$DISPLAY"
application/vnd.x-ms-word; oowriter '%s'; -n "$DISPLAY"
application/dot; oowriter '%s'; -n "$DISPLAY"
application/x-dot; oowriter '%s'; -n "$DISPLAY"
application/x-msword; oowriter '%s'; -n "$DISPLAY"
application/msword; oowriter '%s'; -n "$DISPLAY"
application/rtf; oowriter '%s'; -n "$DISPLAY"
application/x-rtf; oowriter '%s'; -n "$DISPLAY"
text/rtf; oowriter '%s'; -n "$DISPLAY"

# MS Access
application/mdb; oobase '%s'; -n "$DISPLAY"
application/x-mdb; oobase '%s'; -n "$DISPLAY"
application/msaccess; oobase '%s'; -n "$DISPLAY"
application/x-msaccess; oobase '%s'; -n "$DISPLAY"
application/vnd.x-msaccess; oobase '%s'; -n "$DISPLAY"
application/ms-access; oobase '%s'; -n "$DISPLAY"

# MS Excel
application/msexcel; oocalc '%s'; -n "$DISPLAY"
application/ms-excel; oocalc '%s'; -n "$DISPLAY"
application/x-ms-excel; oocalc '%s'; -n "$DISPLAY"
application/x-xls; oocalc '%s'; -n "$DISPLAY"
application/xls; oocalc '%s'; -n "$DISPLAY"
application/x-excel; oocalc '%s'; -n "$DISPLAY"
application/vnd.ms-excel; oocalc '%s'; -n "$DISPLAY"

# PDF and PS & DVI
application/pdf; kpdf '%s'; -n "$DISPLAY"
application/x-pdf; kpdf '%s'; -n "$DISPLAY"
application/postscript; ghostview '%s'; -n "$DISPLAY"
application/x-dvi; ghostview '%s'; -n "$DISPLAY"

# Multimedia
audio/*; amarok '%s'; -n "$DISPLAY"
video/*; kaffeine '%s'; -n "$DISPLAY"

# Java Web Start
application/x-java-jnlp-file; javaws '%s'; -n "$DISPLAY"

# OpenDocument stuff
application/vnd.oasis.opendocument.text-template; oowriter '%s'; -n "$DISPLAY"
application/vnd.oasis.opendocument.text; oowriter '%s'; -n "$DISPLAY"
application/vnd.oasis.opendocument.spreadsheet-template; oocalc '%s'; -n "$DISPLAY"
application/vnd.oasis.opendocument.spreadsheet; oocalc '%s'; -n "$DISPLAY"
application/vnd.oasis.opendocument.presentation-template; ooimpress '%s'; -n "$DISPLAY"
application/vnd.oasis.opendocument.presentation; ooimpress '%s'; -n "$DISPLAY"
application/vnd.oasis.opendocument.image; oodraw '%s'; -n "$DISPLAY"
application/vnd.oasis.opendocument.graphics-template; oodraw '%s'; -n "$DISPLAY"
application/vnd.oasis.opendocument.graphics; oodraw '%s'; -n "$DISPLAY"

# OpenOffice 1.0
application/vnd.sun.xml.calc; oocalc '%s'; -n "$DISPLAY"
application/vnd.sun.xml.calc.template; oocalc '%s'; -n "$DISPLAY"
application/vnd.sun.xml.writer; oowriter '%s'; -n "$DISPLAY"
application/vnd.sun.xml.writer.template; oowriter '%s'; -n "$DISPLAY"
application/vnd.sun.xml.impress; ooimpress '%s'; -n "$DISPLAY"
application/vnd.sun.xml.impress.template; ooimpress '%s'; -n "$DISPLAY"

# Image files
image/*; gwenview '%s'; -n "$DISPLAY"
application/illustrator; gimp '%s'; -n "$DISPLAY"

# Text
plain/text; kwrite '%s'; -n "$DISPLAY"
text/*; kwrite '%s'; -n "$DISPLAY"
application/txt; kwrite '%s'; -n "$DISPLAY"
application/x-shellscript; kwrite '%s'; -n "$DISPLAY"
application/x-sh; kwrite '%s'; -n "$DISPLAY"

# CHM
application/chm; kchmviewer '%s'; -n "$DISPLAY"
application/x-chm; kchmviewer '%s'; -n "$DISPLAY"

# Po files
text/x-pofile; kbabel '%s'; -n "$DISPLAY"

# Bittorrent
application/x-bittorrent; ktorrent '%s'; -n "$DISPLAY"

