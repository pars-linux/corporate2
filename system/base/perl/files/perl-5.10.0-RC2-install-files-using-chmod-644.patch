--- ./lib/ExtUtils/Install.pm.tv	2007-11-27 12:24:37.000000000 +0100
+++ ./lib/ExtUtils/Install.pm	2007-11-27 12:24:50.000000000 +0100
@@ -666,7 +666,7 @@ sub install { #XXX OS-SPECIFIC
             utime($atime,$mtime + $Is_VMS,$targetfile) unless $nonono>1;
 
 
-            $mode = 0444 | ( $mode & 0111 ? 0111 : 0 );
+            $mode = 0644 | ( $mode & 0111 ? 0111 : 0 );
             $mode = $mode | 0222
                 if $realtarget ne $targetfile;
             _chmod( $mode, $targetfile, $verbose );
@@ -1018,7 +1018,7 @@ sub pm_to_blib {
         }
         my($mode,$atime,$mtime) = (stat $from)[2,8,9];
         utime($atime,$mtime+$Is_VMS,$to);
-        _chmod(0444 | ( $mode & 0111 ? 0111 : 0 ),$to);
+        _chmod(0644 | ( $mode & 0111 ? 0111 : 0 ),$to);
         next unless $from =~ /\.pm$/;
         _autosplit($to,$autodir);
     }
