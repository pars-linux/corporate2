Index: ortp-0.16.1/src/stun.c
===================================================================
--- ortp-0.16.1.orig/src/stun.c
+++ ortp-0.16.1/src/stun.c
@@ -1182,7 +1182,7 @@ stunRand(void)
 #elif defined(__MACH__) 
       {
 	int fd=open("/dev/random",O_RDONLY);
-	read(fd,&tick,sizeof(tick));
+	res = read(fd,&tick,sizeof(tick));
 	closesocket(fd);
       }
 #elif defined(__linux) || defined(HAVE_DEV_RANDOM) 
@@ -1191,6 +1191,7 @@ stunRand(void)
 	int maxFd=0;
 	struct timeval tv;
 	int e;
+	int res;
 
         int fd=open("/dev/random",O_RDONLY);
 
